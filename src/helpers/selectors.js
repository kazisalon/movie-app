export const currentPage = state => state.pagination.currentPage;
export const filters = state => state.filters;
export const genreId = state => state.filters.genreId;
export const byRating = state => state.filters.byRating;
export const byPopularity = state => state.filters.byPopularity;
export const inputValue = state => state.filters.inputValue;
